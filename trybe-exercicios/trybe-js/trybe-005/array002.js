const shopping = ['sabao em po', 'macarrao', 'leite condensado', 'refrigerante', 'maça', 'alface', 'nescau'];

for (let index = 0; index < shopping.length; index += 1) {
    //console.log(index);
    //console.log(shopping[index]);
    console.log(`${index} - ${shopping[index]}`);
};